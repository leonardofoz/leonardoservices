server:
  port: 8081
spring:
  application:
    name: fraud
  output:
    ansi:
      enabled: always
  datasource:
    password: password
    url: jdbc:postgresql://localhost:5432/fraud
    username: leonardo
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true
